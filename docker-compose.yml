services:
  game:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./app:/app/app  # Syncs your code changes instantly (Hot Reload)
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload